#include "Graphics3d.hpp"

/*NAMESPACE {

  Graphics3d::addNode(Node* node) {
    nodes.push_back(node);
  }
  Graphics3d::setCamera(Camera* camera) {
    cam = camera;
  }
  Graphics3d::setShader(String name) {
    shade = Shader(name);
    shade.use();
  }
  Graphics3d::render(GLFWWindow* window) {
    glClear(GL_COLOR_BUFFER_BIT | GL_DEPTH_BUFFER_BIT);
    for (Node* node : nodes) {
      node->render();
    }
  }

}*/
